{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run App in VS Code (F5)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "npm: dev"
    }
  ]
}
